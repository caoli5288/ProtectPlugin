name: ${project.name}
version: ${project.version}
main: com.mengcraft.server.Protect
commands:
  protect:
    description: admin command for this
    permission: server.admin
permissions:
  server.admin:
     description: admin permission for this
     default: op